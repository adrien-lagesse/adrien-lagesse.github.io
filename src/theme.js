import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette:{
      primary:{
        main: "#001F54",
        light: "#C0D2F2"
      },
      secondary:{
        main: "#f1faee",
      }
    }
  })

export default theme;